# rayTracing
Summer '25 Project 

C++ with SDL2 library

## Instructions
To build the project, run the following command:

```bash
make all
```

This will create the `bin` and `obj` directories

now run:
```bash
bash run.sh
```

